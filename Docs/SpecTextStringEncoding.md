# Spec — Library-wide Text-String Encoding (PDFDocEncoding ⇄ UTF-16BE)

> **Status:** design approved 2026-06-20. LOCAL/untracked — delete on merge.
> **Goal:** text strings round-trip arbitrary Unicode correctly (create → serialize → parse → read),
> using true PDFDocEncoding when representable and UTF-16BE-with-BOM otherwise. Correctness item
> gating the 1.0.0 tag (the last open correctness gap; Q2 in the edit-suite track).

## Problem

`PdfString(string)` and the implicit `string → PdfString` operator encode via `Encoding.Latin1`,
which silently corrupts any character outside U+0000–U+00FF (chars above the range become `?`).
The read path (`Value`) is Latin-1 with no BOM detection. The only place text is handled correctly
today is an **ad-hoc UTF-16BE codec inlined in `PdfOutlineItem`** (encode prepends `FE FF`; decode
sniffs it, else falls back to `Value`). Every other §7.9.2 text site — document Info (Title, Author,
Subject, Keywords, Creator, Producer via `PdfMetadata.SetInfoString`), page-label `/P` prefix,
annotation `/Contents`, and the builder-side Info/bookmark titles — goes through the Latin-1 path and
corrupts. No real PDFDocEncoding table exists in the codebase (the "Latin-1 == PDFDocEncoding"
equivalence is only ever a code comment).

PDF has two string worlds (ISO 32000 §7.9.2):
- **Text strings** — Info values, outline titles, annotation `/Contents`, form field `/V`. Encoded as
  PDFDocEncoding **or** UTF-16BE (with a leading `U+FEFF`). PDF 2.0 also permits UTF-8 (leading
  `EF BB BF`).
- **Byte strings** — the trailer `/ID`, content-stream show-strings, encryption values, name-tree
  keys (incl. named-destination *names*). Raw bytes; must **never** pass through text encoding.

Correctness lives at the call-site classification (text vs bytes), not in the constructor.

## Decisions (approved)

1. **Encoding model — true PDFDocEncoding, else UTF-16BE.** On write, if every character maps to
   PDFDocEncoding (ISO 32000-2 Annex D.2 character set), emit single-byte PDFDocEncoding; otherwise
   emit UTF-16BE prefixed with the `FE FF` BOM. This is byte-optimal and matches conventional
   producer output (e.g. Acrobat). Both forms are spec-legal for any text string.

2. **API shape — A+ (hardened additive).** Add a text path; remove the silent write-side foot-gun;
   leave the byte/read primitives alone. See "PdfString changes" below.

3. **UTF-16BE serializes as hexadecimal** `<FEFF…>` (avoids escaping binary inside a literal);
   PDFDocEncoding/ASCII strings serialize as **literal** `(…)`. UTF-8 is **decoded** (PDF 2.0 inputs)
   but **never emitted** — UTF-16BE is the universal write form. The existing octal-escape fix in
   `ToLiteralString` remains the literal-string safety net.

4. **`Value` is unchanged** (Latin-1, byte-faithful). It is correct for content-stream show-strings
   and byte strings; text contexts use the new `GetText()`.

## Components

### `PdfDocEncoding` — new static class, `PdfLibrary/Core/Primitives/PdfDocEncoding.cs`

Owns the encoding policy in one place. Replaces the inlined codec in `PdfOutlineItem`.

```csharp
internal static class PdfDocEncoding
{
    /// PDFDocEncoding if every char is representable; otherwise UTF-16BE with a leading FE FF BOM.
    public static byte[] Encode(string text);

    /// BOM-sniff: FE FF → UTF-16BE; EF BB BF → UTF-8 (read-only, PDF 2.0); else PDFDocEncoding.
    public static string Decode(ReadOnlySpan<byte> bytes);

    /// True iff every char maps to a PDFDocEncoding byte (drives the Encode branch + tests).
    public static bool IsRepresentable(string text);
}
```

- Backing tables: a `char → byte` map (for encode/representability) and a `byte → char` map (for
  decode), built from the **ISO 32000-2 Annex D.2** PDFDocEncoding character set. The table MUST be
  byte-correct at the points where PDFDocEncoding diverges from Latin-1:
  - `0x18–0x1F` — typographic chars (breve, caron, circumflex, dotaccent, hungarumlaut, ogonek, ring,
    tilde) where Latin-1 has C0/C1-adjacent control codes.
  - `0x7F` and the `0x80–0x9F` band — PDFDocEncoding assigns printable/typographic chars (bullet,
    dagger, ellipsis, em/en dash, quotes, ligatures fi/fl, Ł Œ Š Ÿ Ž ı ł œ š ž, etc.) where Latin-1
    has C1 control codes.
  - `0xA0` and any other Annex-D-specific assignments.
  - The exact 256-entry table is fetched from the spec (pdf-rag) during planning and embedded in the
    plan; a unit test pins the divergent code points so a wrong entry fails loudly.
- Unmapped bytes on **decode** map to the Unicode replacement char `U+FFFD` (tolerant, never throws).
- A char that is not representable triggers the whole-string UTF-16BE branch on **encode** (we never
  mix encodings within one string).
- Decode of a bare `FE FF` (2 bytes, empty UTF-16) → empty string; `EF BB BF` only → empty string.

### `PdfString` changes — `PdfLibrary/Core/Primitives/PdfString.cs`

- **Add** `public static PdfString FromText(string text)` →
  `new PdfString(PdfDocEncoding.Encode(text), formatChosenByContent)` where the format is
  **Hexadecimal** when the encoded bytes start with `FE FF`, else **Literal**.
- **Add** `public string GetText()` → `PdfDocEncoding.Decode(_bytes)`.
- **Remove** the implicit `operator PdfString(string)` (the silent write-side foot-gun). The compiler
  then flags every implicit-conversion site for explicit migration — exhaustive and mechanical.
- **Rename / reclassify** the Latin-1 `PdfString(string)` ctor: replace with an explicitly named
  byte-literal factory `public static PdfString FromByteLiteral(string asciiOrLatin1)` (same Latin-1
  body). Intent is now named at the call-site; it can never be mistaken for text encoding. (Sites
  that were genuinely ASCII — names-as-strings, tokens — use this.)
- **Keep unchanged:** `PdfString(byte[])`, `Bytes`, `Value` (Latin-1), the
  `implicit operator string(PdfString)` read path, `Equals`/`GetHashCode`, `ToLiteralString`
  (with its octal-escape fix), `ToHexadecimalString`.

### Call-site migration (the audit)

Driven by (a) the compiler errors from removing the implicit operator and (b) a deliberate sweep of
§7.9.2 text sites. Each text **write** site switches to `PdfString.FromText`; each text **read** site
switches `.Value`/implicit-string to `GetText()`.

**Text WRITE sites → `FromText`:**
- `PdfMetadata.SetInfoString` → Title, Author, Subject, Keywords, Creator, Producer.
  (`SetInfoDate` stays `FromByteLiteral` — PDF date strings are ASCII.)
- `PdfOutlineItem` title (delete its inlined UTF-16BE codec; route through `FromText`).
- `PdfPageLabels` `/P` prefix.
- `PdfPageAnnotator` `/Contents` (AddNote) and any annotation title/contents text.
- Builder side in `PdfDocumentWriter`: creation-time Info dict values + bookmark titles.

**Text READ sites → `GetText`:**
- The matching getters in `PdfMetadata`, `PdfOutlineItem.GetTitle`, `PdfPageLabels` prefix read.

**Explicitly EXCLUDED — stay byte (`FromByteLiteral` / raw bytes, NOT text):**
- Trailer `/ID`; content-stream show-strings (`Tj`/`TJ` operands); encryption values; name-tree keys
  including **named-destination names** (`PdfNamedDestinations` — compared by byte value, not display).
- `#4` form-field `/V` is a text site but is **out of scope here** (noted for when form filling lands).

### Encryption & parser ordering (unchanged — confirming soundness)

Write: text-encode → (optional) encrypt bytes → serialize. Read: parse → decrypt bytes inline
(`PdfParser`) → `GetText()`. Encryption wraps the post-text-encoding bytes; no interaction. The
lexer/parser core is **not** modified — it already stores raw string bytes faithfully.

## Error handling

- Decode never throws: unmapped PDFDocEncoding bytes → `U+FFFD`; malformed/odd-length UTF-16BE →
  best-effort via `Encoding.BigEndianUnicode` (which is itself tolerant).
- Encode never throws: any non-representable char routes the whole string to UTF-16BE, which covers
  all of Unicode.
- All encode/decode is culture-independent (byte tables + fixed `Encoding` instances); no
  `CultureInfo`-sensitive calls.

## Testing

- **Codec round-trip** (`PdfDocEncodingTests`): ASCII; accented Latin (exercises single-byte
  PDFDocEncoding); CJK + emoji (forces UTF-16BE) — `Encode → Decode` equality, and full
  `FromText → ToPdfString → parse → GetText` equality, each run under **InvariantCulture, de-DE,
  fr-FR**.
- **Annex D fidelity:** assert the specific divergent code points (`0x18–0x1F`, `0x7F`, `0x80–0x9F`,
  `0xA0`) decode to the correct characters and that those characters encode back to the same bytes.
- **Decode BOM-sniff:** `FE FF …` → UTF-16BE; `EF BB BF …` → UTF-8; bare bytes → PDFDocEncoding;
  empty/`FE FF`-only → empty string.
- **Format selection:** `FromText` of a UTF-16BE-requiring string serializes as `<FEFF…>`
  hexadecimal; an ASCII/PDFDocEncoding string serializes as a `(…)` literal.
- **Non-regression:** trailer `/ID` round-trips byte-identical; content-stream `Tj` text extraction
  unaffected (still reads via `Value`); a build/grep check that **zero** implicit
  `string → PdfString` usages remain.
- **End-to-end (the original bug):** set a CJK `Title` (and an accented-Latin `Author`) via
  `edit.Metadata`, `Save`, reload, read back identical — under a non-invariant culture too.
- Full `PdfLibrary.Tests` suite stays green.

## Out of scope

- Form-field `/V` text encoding (arrives with edit #4 form filling — will reuse `FromText`).
- File-specification string encodings (`/F` vs `/UF`), collection/`/Desc` fields — not currently
  authored by the library.
- Changing the read semantics of `Value` or the content-stream text-extraction path.
