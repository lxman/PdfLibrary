<Window x:Class="ImageUtility.CodecSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Codec Settings" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Configure Codec Preferences"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Settings Grid -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="SettingsPanel">
                    <!-- Format settings will be added dynamically here -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Reset to Defaults" Click="ResetToDefaults_Click"
                    Width="120" Height="30" Margin="0,0,10,0"/>
            <Button Content="Save" Click="Save_Click"
                    Width="80" Height="30" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Cancel" Click="Cancel_Click"
                    Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
